name: Gatekeeper_Approvals

on:
  workflow_dispatch:
  pull_request_review:
    types: [submitted, dismissed]
    branches: [master]   # перевір, чи точно master

permissions:
  contents: read
  pull-requests: read

jobs:
  approvals_gate:
    runs-on: ubuntu-latest
    if: >-
      ${{
        github.event.action == 'dismissed' ||
        (github.event.action == 'submitted' && github.event.review.state != 'commented')
      }}
    steps:
      - run: echo "Review event passed filter (not commented)"

  resolve_ct_inputs:
    # Потрібно лише якщо approvals_gate відбувся
    needs: approvals_gate
    runs-on: ubuntu-latest
    outputs:
      pr_number: ${{ steps.out.outputs.pr_number }}
      head_sha:  ${{ steps.out.outputs.head_sha }}
    steps:
      - id: out
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const owner = context.repo.owner;
            const repo  = context.repo.repo;

            let prNumber = context.payload?.pull_request?.number ?? null;
            let headSha  = context.payload?.pull_request?.head?.sha ?? null;

            // Якщо це review-івент, але чомусь head_sha нема — візьмемо з review/sha, або з github.sha
            if (!headSha) {
              headSha = context.payload?.review?.commit_id || process.env.GITHUB_SHA || null;
            }

            // Якщо prNumber досі нема — спробуємо знайти PR за headSha
            if (!prNumber && headSha) {
              const prs = await github.paginate(
                github.rest.repos.listPullRequestsAssociatedWithCommit,
                { owner, repo, commit_sha: headSha, per_page: 100 }
              );
              prNumber = prs[0]?.number ?? null;
            }

            core.notice(`Resolved pr_number=${prNumber || 'null'} head_sha=${headSha || 'null'}`);

            core.setOutput('pr_number', prNumber ? String(prNumber) : '');
            core.setOutput('head_sha',  headSha  ? String(headSha)  : '');

  run_ct:
    needs: [approvals_gate, resolve_ct_inputs]
    uses: ./.github/workflows/component-tests.yml
    with:
      pr_number: ${{ needs.resolve_ct_inputs.outputs.pr_number }}
      head_sha:  ${{ needs.resolve_ct_inputs.outputs.head_sha }}
    permissions:
      contents: read
      pull-requests: read




  # approvals_gate:
  #   runs-on: ubuntu-latest
  #   # Ігноруємо comment-only review, обробляємо dismissed і approve/changes_requested
  #   if: >-
  #     ${{
  #       github.event.action == 'dismissed' ||
  #       (github.event.action == 'submitted' && github.event.review.state != 'commented')
  #     }}
  #   outputs:
  #     approvals: ${{ steps.gate.outputs.approvals }}
  #     blockers:  ${{ steps.gate.outputs.blockers }}
  #     passed:    ${{ steps.gate.outputs.passed }}
  #   steps:
  #     - name: Evaluate approvals / blockers
  #       id: gate
  #       uses: actions/github-script@v7
  #       with:
  #         github-token: ${{ secrets.GITHUB_TOKEN }}
  #         script: |
  #           const pr = context.payload.pull_request;
  #           if (!pr) {
  #             core.notice('No PR context; exiting gate with passed=false');
  #             core.setOutput('approvals', '0');
  #             core.setOutput('blockers',  '1'); // вважаємо що є блокер, щоб не запускати тести
  #             core.setOutput('passed',    'false');
  #             return;
  #           }

  #           const allReviews = await github.paginate(
  #             github.rest.pulls.listReviews,
  #             {
  #               owner: context.repo.owner,
  #               repo: context.repo.repo,
  #               pull_number: pr.number,
  #               per_page: 100,
  #             }
  #           );

  #           const latestByUser = new Map();
  #           for (const review of allReviews) {
  #             if (review.user && ['APPROVED', 'CHANGES_REQUESTED', 'DISMISSED'].includes(review.state)) {
  #               latestByUser.set(review.user.id, review.state);
  #             }
  #           }

  #           const approvals = [...latestByUser.values()].filter(s => s === 'APPROVED').length;
  #           const blockers  = [...latestByUser.values()].filter(s => s === 'CHANGES_REQUESTED').length;

  #           const needApprovals = 1; // винеси у vars/ENV за потреби
  #           const passed = approvals >= needApprovals && blockers === 0;

  #           core.notice(`Approvals: ${approvals}/${needApprovals}; Blockers: ${blockers}; Gate passed: ${passed}`);
  #           // ВАЖЛИВО: НЕ викликати core.setFailed() — job має завершитися успішно
  #           core.setOutput('approvals', String(approvals));
  #           core.setOutput('blockers',  String(blockers));
  #           core.setOutput('passed',    String(passed));

